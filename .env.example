N8N_WEBHOOK_URL=https://chileanrentacar.app.n8n.cloud/webhook/guardar-patente
CORS_ORIGINS=capacitor://localhost,http://localhost

# Optional. Leave empty to use a temporary Cloudflare Quick Tunnel.
# For a stable URL, create a Cloudflare Tunnel and paste its token here.
CLOUDFLARED_TUNNEL_TOKEN=